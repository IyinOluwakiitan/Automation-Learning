package src.basics_02;

public class Lab014 {
    public static void main(String[] args) {
        int Enum = 9;
        System.out.println(Enum);

      // byte b = 128; this will not run because it is out of range rather it will run if we use int
        //int a,b,c = 1,2,3 this will not run instead do this
       int a = 1, b = 2, c = 10; //(multiple initilization) this will run

        boolean bool = true;
        boolean bool2 = false; // FALSE or TRUE in caps will not run, 1234 will not run
        //only 2 unique values true / false in lower case will run

       //literal is the actual value
        //null is only assigned to reference type of variable which are non-primitive reference type

    }
}
