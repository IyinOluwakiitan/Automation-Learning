package src.basics_02;

public class Lab017 {
    public static void main(String[] args) {
        //Operators contd
        //byte + byte = Int
        byte a = 10;
        byte b = 18;
        System.out.println(a+b); //the result becomes integer
        //char + char = int / short + byte = int
        //to remember quickly know that the bigger one out of both would keep the value
        //default value of boolean is false... se are learning on the local for now
    }
}
